# pynotify

`pynotify` provides a terminal interface for composing and sending Adaptive Card
messages to selected Microsoft Teams channels.

Run `pynotify`, select one or more channels, enter a header and message, and
choose **Send to selected channels**.

See [Configuration](configuration.md) to configure your channel names without
storing webhook credentials in the project.
